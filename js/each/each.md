@function can-util/js/each each
@description An each
@signature `each(elements, callback, context)`
@param {Object|ArrayLike} elements
@param {function(element, key, elements)} callback
@param {Object} [context] the context object
